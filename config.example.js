// config.example.js - szablon konfiguracyjny (umieść w repozytorium)
// Skopiuj ten plik jako config.js i uzupełnij klucz API

const config = {
    googleMapsApiKey: 'YOUR_API_KEY_HERE'
};

// Nie zmieniaj poniższej linii
typeof module !== 'undefined' ? module.exports = config : null;